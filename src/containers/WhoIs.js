import React from 'react';
import '../App.css';

const WhoIs = (props) => {
    
        return (
             <div>
                 <h4>{props.data}</h4>
             </div>
        );
}

export default WhoIs;